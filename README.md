# LSTM-Time-Series-Forecasting---AirPassengers
This project uses an LSTM model to forecast airline passenger data. It preprocesses time series data, trains a neural network, evaluates performance, and predicts future values.
